{% extends 'DemoTutorialBundle:Default:album_base.html.twig' %}

{% block body %}

<h1>{{ title }}</h1>
<form action="{{ path('_album_add_save') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <input type="submit" name="add" value="Add" />
    <input type="submit" name="add" value="Cancel" />
</form>

{% endblock body %}
